export const EXTENSION_ID = "vscode-shell";

export const FORMAT_SECTION = `${EXTENSION_ID}.format`;
export const FORMAT_OUTPUT_CHANNEL = "Shell Format";

export const SHELLCHECK_SECTION = `${EXTENSION_ID}.shellcheck`;
export const SHELLCHECK_COMMAND_PREFIX = `${EXTENSION_ID}.shellcheck`;
export const SHELLCHECK_OUTPUT_CHANNEL = "ShellCheck";
export const SHELLCHECK_LINK_LANGUAGE = "shellcheck-output";
